{
  "name": "wp-module-loader",
  "version": "1.1.0",
  "engines": {
    "node": ">=22.11.0",
    "npm": ">=10.9.0"
  },
  "scripts": {
    "build": "echo \"No build step required\" && exit 0"
  },
  "devDependencies": {
    "@wordpress/env": "^10.20.0"
  }
}
